Concepto de TAD: 

Tipo Abstracto de Dato, es algo que no existe y por tanto es un concepto.
Nos permite crear nuestros propios tipos de datos, como lo son los string, int, bool...

En este ejemplo, vamos a crear un tipo de dato, TIPO FECHA.
Ya sabemos que para crear un nuevo tipo de dato, debemos crear además del dato, un conjunto de operaciones que la encapsulen.

Por tanto, primero necesitamos saber un nuevo concepto que es, la palabra Struct.
La estructura, agrupa datos, que en definitiva son variables.

